export enum Segments {
  PARAMS = "params",
  HEADERS = "headers",
  QUERY = "query",
  COOKIES = "cookies",
  SIGNEDCOOKIES = "signedCookies",
  BODY = "body"
}

export enum Modes {
  FULL = "full",
  PARTIAL = "partial"
}
