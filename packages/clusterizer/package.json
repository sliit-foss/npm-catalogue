{
    "name": "@sliit-foss/clusterizer",
    "version": "1.1.2",
    "description": "A wrapper around the node cluster module to make it easier to bootstrap a clusterized application",
    "main": "dist/index.js",
    "scripts": {
        "build": "esbuild src/index.js --bundle --platform=node --target=node14 --outfile=dist/index.js --sourcemap --minify",
        "bump-version": "pnpm build && npx automatic-versioning --name=@sliit-foss/clusterizer --no-commit --recursive",
        "lint": "eslint src --ext .js --fix",
        "release": "pnpm run bump-version && pnpm publish --access=public --tag=latest || true",
        "test": "shx cp ../../.babelrc ../../jest.config.js . && dotenv -- jest --coverage --verbose --runInBand --forceExit && rimraf .babelrc jest.config.js"
    },
    "author": "SLIIT FOSS",
    "license": "MIT",
    "repository": {
        "type": "git",
        "url": "git+https://github.com/sliit-foss/npm-catalogue.git"
    },
    "homepage": "https://github.com/sliit-foss/npm-catalogue/blob/main/packages/clusterizer/readme.md",
    "keywords": [
        "clusterizer",
        "node:cluster",
        "multi-processing"
    ],
    "bugs": {
        "url": "https://github.com/sliit-foss/npm-catalogue/issues"
    }
}
