{
    "name": "@sliit-foss/mongoose-filter-query",
    "version": "1.1.0",
    "description": "Middleware which implements a standardized format and maps an incoming http request's query params to a format which is supported by mongoose",
    "main": "dist/index.js",
    "scripts": {
        "build": "esbuild src/index.js --bundle --platform=node --target=node14 --outfile=dist/index.js --sourcemap --minify",
        "bump-version": "pnpm build && npx automatic-versioning --name=@sliit-foss/mongoose-filter-query --no-commit --recursive",
        "lint": "eslint src --ext .js --fix",
        "release": "pnpm run bump-version && pnpm publish --access=public --tag=latest || true",
        "test": "shx cp ../../.babelrc ../../jest.config.js . && dotenv -- jest --coverage --verbose --runInBand --forceExit && rimraf .babelrc jest.config.js"
    },
    "author": "SLIIT FOSS",
    "license": "MIT",
    "repository": {
        "type": "git",
        "url": "git+https://github.com/sliit-foss/npm-catalogue.git"
    },
    "homepage": "https://github.com/sliit-foss/npm-catalogue/packages/mongoose-filter-query#readme",
    "keywords": [
        "filterquery",
        "sortquery",
        "filter",
        "sort"
    ],
    "bugs": {
        "url": "https://github.com/sliit-foss/npm-catalogue/issues"
    }
}
